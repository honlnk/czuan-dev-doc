## 流程图

``` mermaid
graph TD
    A[开始] --> B[从数据库中查找已有的翻译数据]
    B --> C[遍历输入数据，筛选出未找到的翻译]
    C --> D[将未找到的翻译批量插入数据库]
    D --> E[收集所有翻译ID（已存在的+新插入的）]
    E --> F[根据ID列表查询完整的翻译信息]
    F --> G[将翻译信息按词性分组，构建Map结构]
    G --> H[返回结果]

```
